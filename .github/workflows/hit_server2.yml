name: Ping Render Backend2

on:
  schedule:
    - cron: '0 6-17 * * *' # Every hour from 06:00 to 17:00 UTC = 9a–8p Finland time
  workflow_dispatch: # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend Server
        run: curl -k -f "https://bagent.onrender.com/health" || true
